#include <stdlib.h>
#include <iostream>
#include <string.h>

class Helper {
private:
    Picture *scanedPicture;
    cvClass *cvRunner(Picture *scanedPicture, Object obj);
    Square *squareObject;
    glClass *glRunner(Object *obj);
}

public:
class doScan(Picture *scannedPicture, Object obj)
{
    this.cvRunner(Picture *scanedPicture, Object *object);
    this.glRunner(Object *squareObject);
}
